services:
  - type: web
    name: resume-app
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: gunicorn app:app
    envVars:
      - key: GOOGLE_API_KEY
        sync: false